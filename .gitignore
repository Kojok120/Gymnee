# Xcode / build
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout

# XcodeGen generates this from project.yml (source of truth)
Gymnee.xcodeproj/

# macOS
.DS_Store

# Swift Package Manager
.build/
.swiftpm/

# Secrets (copy Config/Secrets.example.xcconfig → Secrets.dev/prod.xcconfig and fill in)
Config/Secrets.dev.xcconfig
Config/Secrets.prod.xcconfig
Gymnee/Resources/Supabase.plist

# APNs auth key (.p8) and any secrets dir — never commit private keys
secrets/
*.p8

# Misc
*.log

# 楽天API生成カタログ（再生成可・applicationIdを含む）
supabase/catalog_from_rakuten.sql
