import Foundation

/// 計画（PlannedWorkout）の既定タイトル。
///
/// カスタムセット（Routine）廃止前は「ユーザーが作ったセット名」も計画タイトルの供給源だったが、
/// 廃止後はこの定番リストが唯一の供給源になる。週プランナーと日別詳細で同じ並びを出すため、
/// また AI 計画にも同じ語彙を渡すために 1 か所に置く。
enum PlanTitles {
    static let presets = ["胸の日", "背中の日", "脚の日", "肩・腕", "全身", "有酸素", "休養"]
}
